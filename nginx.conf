# Nginx Configuration for Lena Bot Webhook
# Add this configuration to your existing nginx setup

# If using a separate subdomain (recommended):
# server {
#     listen 80;
#     server_name lena-bot.yourdomain.com;
#
#     # Redirect to HTTPS
#     return 301 https://$server_name$request_uri;
# }
#
# server {
#     listen 443 ssl http2;
#     server_name lena-bot.yourdomain.com;
#
#     # SSL certificates (replace with your actual paths)
#     ssl_certificate /etc/letsencrypt/live/lena-bot.yourdomain.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/lena-bot.yourdomain.com/privkey.pem;
#
#     # SSL settings
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     # Webhook endpoint
#     location /bot/webhook {
#         proxy_pass http://localhost:5002/bot/webhook;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#
#         proxy_read_timeout 60;
#         proxy_connect_timeout 60;
#     }
#
#     # Health check endpoint
#     location /bot/health {
#         proxy_pass http://localhost:5002/bot/health;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#     }
# }

# If adding to existing domain (e.g., api.bii.kz):
# Add these location blocks to your existing server block:

# Lena Bot Webhook endpoint
location /api/lena-bot/webhook {
    proxy_pass http://localhost:5002/bot/webhook;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;

    proxy_read_timeout 60;
    proxy_connect_timeout 60;
}

# Lena Bot Health check
location /api/lena-bot/health {
    proxy_pass http://localhost:5002/bot/health;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
